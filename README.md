# Nest.js Example: Caster API

## Local Development

### DB Setup

Use `docker-compose` to launch a local database by running:

```sh
nx docker.up
```

Then, run migrations to catch your schema up to the latest:

```sh
yarn db:migrate
```
